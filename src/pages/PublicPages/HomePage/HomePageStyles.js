import { styled } from '@mui/system'
import { Box } from '@mui/material'

export const HomePageImage = styled(Box)({
  width: '100%',
  height: '100%',
  backgroundRepeat: 'no-repeat',
  objectFit: 'contain',
})
